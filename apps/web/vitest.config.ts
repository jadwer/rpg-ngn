import { definePackageConfig } from '../../vitest.shared'

/** Solo logica pura (pack empaquetado, fichas, preparacion de la mesa, voz); la pantalla se prueba en el navegador. */
export default definePackageConfig()
